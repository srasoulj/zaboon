# Environment files: never commit secrets (see .env.example for variable names)
.env
.env*.local

# Dependencies and build output
node_modules/
.next/
.turbo/
dist/
coverage/
*.tsbuildinfo
next-env.d.ts

# Local runtime state (DB data lives outside the repo; logs/caches here)
.local/
.cache/

# Test output
test-results/
playwright-report/
blob-report/

# OS / editor
.DS_Store

# Local content builds (content-cli publish --local)
apps/web/public/content/

SPEC: `ws-engine` — implement `packages/game-rules`, `packages/srs` and `packages/session-engine` for real.

Read: docs/ARCHITECTURE.md §6 (game rules), docs/LEARNING-ENGINE.md §5 (session engine), §6 (challenge catalogue), §8 (spaced repetition), docs/adr/0006 and 0007; `packages/contracts/src/schemas.ts` (AppConfig defaults, Challenge union — every builder must produce objects that parse with `Challenge`); the stubs in each package (public APIs are the contract); the READ-ONLY oracles in packages/game-rules/oracles/*.yaml and packages/srs/oracles/ratings.yaml; the frozen fixture course in content/fixtures (its lesson specs pin every MVP challenge type).

DELIVER
1. game-rules: exact behavior per the oracles (local-date clamp + tz, tz-change acceptance, streak applyActivity/streakView with freezes, hearts regen/loss/reward/idempotency helpers incl. commit reconciliation `extraHeartsToCharge(serverWrong, recordedEvents)`, XP + daily goal, league placement + rollover, deterministic daily quests `dailyQuests(userId, date, cfg)` from a small template list). Add an energy `LivesPolicy` implementation behind the same interface (not used by default). The oracle runners currently do some rule logic caller-side because the stub API has no function for it (see the comments in the runners): export helpers so ws-api never re-implements rules — `clampActivityTime(at, now)` (the clamped instant behind `localDateFor`), hearts `settleCommit({kind, serverWrong, recordedEvents, …})` with practice exemption, (sessionId, attemptSeq) idempotency and `extra`, and srs `outcomesByItem(answers)` (skipped ignored, passing verdicts, slowest passing ms, null when all skipped). Add tests that feed the oracle YAML cases through these helpers and get identical results.
2. srs: wire ts-fsrs (FSRS-6) for newCard/review/retrievability; keep ratingFor/strengthBars behavior.
3. session-engine: builders for ALL 13 MVP challenge types (select_image, select_translation, translate_bank, translate_type, match_pairs, listen_tap, cloze_choice, complete_chat, letter_intro, letter_sound, letter_forms, read_word, build_word) from content (ContentView), deterministic by seed; the new-word ladder (NEW WORD intro → recognition → production bank → listening), mix profiles from AppConfig.mixProfiles, due-review mixing (reviewShare) from learner FSRS cards, open-mistake mixing, distractor rules (same part of speech, similar length, never an accepted variant — use `@zaboon/grader` `accepts`), letters sessions from the letters track, `isNew` flags, skip types whose media is missing (e.g. select_image without images). `rebuildChallenges(refs)` must reproduce `generateSession` output exactly.
4. Oracles: the runners already exist and are READ-ONLY like the YAML (`packages/game-rules/oracles/game-rules.oracle.test.ts`, `packages/srs/oracles/ratings.oracle.test.ts`; 185 cases). They are skipped until you flip `IMPLEMENTATION` to `'real'` in all three packages; then every case must pass. Rulings encoded there win over the prose: a freeze is granted only when one is actually added (never at the cap); practice never costs hearts and rewards +1 (a user at 0 hearts ends practice at 1); a lastActiveDate after today reads as "extended"; StreakView.freezes is the stored count; clock skew clamps regeneration to 0; skipped attempts are ignored for SRS ratings (all-skipped → null). All oracle cases must pass; disputed cases → `## Contract change request`, never edit oracles.
5. Tests: property tests (fast-check) for determinism, challenge schema validity across random seeds on the fixture course and on content/fa-en, streak invariants (current ≤ longest, freezes ≤ max). Load the fixture course YAML directly in tests (use the `yaml` package; compile answer graphs with `@zaboon/grader` `compile`) — do not depend on the content CLI.

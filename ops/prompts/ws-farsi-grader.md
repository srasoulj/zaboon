SPEC: `ws-farsi-grader` — implement `packages/farsi` and `packages/grader` for real.

Read: docs/LEARNING-ENGINE.md §1 (esp. §1.5 normalization pipeline, §1.4 keyboards), §3 (accepted-answer patterns + automatic merges), §7 (grader verdicts + algorithm); the stubs in packages/farsi/src/index.ts and packages/grader/src/index.ts (their public APIs are the contract: keep signatures, you may add exports); the READ-ONLY oracles packages/farsi/oracles/normalize.yaml and packages/grader/oracles/golden.yaml (+ README).

DELIVER
1. farsi: the full ordered, versioned 7-step normalization pipeline (`normalize` strict form, `looseKey`), port Hazm's constants.py affix/translation/diacritics tables (MIT) and use @persian-tools/persian-tools where it helps; tokenizer; digits; letterForms; SAME_SOUND_GROUPS; complete KEYBOARD_LAYOUTS: `standard` (ISIRI 9147, map KeyboardEvent.code → base/shift chars incl. a half-space key) and `phonetic` (grouped by sound with long-press `variants`, e.g. KeyZ → ز with variants ذ ض ظ; KeyS → س with ص ث; KeyT → ت with ط; KeyH → ه with ح; KeyQ/"gh" → غ with ق), documented in code.
2. grader: `compile()` with automatic merges (pronoun drop for sentence-initial من تو او ما شما آنها اون اونا unless pronounDrop:false; formal register; orthography variant sets; English normalization: lowercase, punctuation, contractions, US/UK spelling, number words ↔ digits); `grade()` with min-cost alignment over the DAG (dynamic programming over DAG node × answer position), verdicts correct / typo (≤1 edit for 4+ letters, ≤2 for 8+; آ/ا counts as typo) / spelling (same-sound letter swap at any length; wrong if it lands on a `lexicon` word) / wrong; `closestSolution` in the learner's register; whole-word `diff`; bank mode = exact DAG membership only. Performance: grading a typical sentence must take < 5 ms; add a guard for huge DAGs.
3. Oracles: the runners already exist and are READ-ONLY like the YAML (`packages/farsi/oracles/normalize.oracle.test.ts`, `packages/grader/oracles/golden.oracle.test.ts`; 136 + 320 cases). They are skipped until you flip both packages' `IMPLEMENTATION` to `'real'`; then every case must pass. The oracle headers pin rules the prose leaves open — they win over the docs: negation (adding/removing the prefix ن / نمی) is never a typo (tip: detect it by stripping a leading ن from both tokens before measuring the edit); a typo OR a same-sound swap that lands exactly on a `lexicon` word is wrong; word-final ه‌ی folds like ۀ/هٔ in the loose key (ه‌ای does not); punctuation is stripped before whitespace collapse; English treats ’ ‘ ʼ as '. Anything the oracles leave open (e.g. «خونه ی من» with a space, transpositions in 4–7 letter words, which length sets the edit limit) is your call: document it in the PR. Every oracle case must pass; if you believe an oracle case is wrong, do NOT edit it — keep the implementation faithful to the docs, mark nothing as skipped, and list the disputed case ids with reasoning under `## Contract change request`.
4. Property tests with fast-check: normalize is idempotent; looseKey(normalize(x)) === looseKey(x); grade(compile([p]), canonical) is always `correct`.
5. Keep the existing `*.contract.test.ts` passing.

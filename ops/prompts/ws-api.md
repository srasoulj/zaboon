SPEC: `ws-api` — implement every MVP API route on top of the walking skeleton.

Read: CLAUDE.md (rules 2, 3, 8, 9), docs/adr/0009 and 0007, docs/ARCHITECTURE.md §3 (system + lesson lifecycle), §5 (data model), §6 (game rules), §7 (API, rate limits, anti-cheat), §9 (security), §10 (key flows: first lesson, outbox replay, link-or-merge, content pickup), §15 (acceptance tests); docs/LEARNING-ENGINE.md §5, §7.3, §8; `packages/contracts/src/{routes,schemas}.ts` (READ-ONLY: every route's request/response contract); `packages/db/src/repos/*` (use these; never raw SQL in routes); the skeleton you extend: `apps/web/lib/server/{sessions,home,content,regrade}.ts` and `apps/web/app/api/**`. The framework files `with-route.ts`, `auth/**`, `env.ts`, `clock.ts`, `errors.ts`, `db.ts`, `config.ts` and `e2e/skeleton.api.spec.ts` are READ-ONLY and must keep passing.

DELIVER (every route in `routes` with phase 'mvp', built with `withRoute(routes.<name>, …)`):
1. `POST /api/onboarding` (13+ gate: `ageConfirmed: true` required; saves reason/selfLevel/dailyGoalXp into settings, timezone via acceptTzChange, onboarded; enrolls in DEFAULT_COURSE_ID at the current version) → HomeResponse. `GET /api/home` (keep; the active course is the most recently used enrollment).
2. `GET /api/path`: sections → units → levels with `lessonsDone/lessonsTotal` and state locked | available | current | completed | legendary. Rules: the first unfinished level is `current`; everything after it is `locked`; completed levels stay replayable. Lazy path migrations: when an enrollment's content_version is older than the current version, apply the manifest `pathMigrations` to level_progress and bump the enrollment (ARCHITECTURE §10.4).
3. `GET /api/letters` (letter strength 0–4 from letter FSRS cards via srs `retrievability` + `strengthBars`, lessons with states) and `GET /api/words` (the learner's lexeme cards with strength and due date).
4. `GET/PATCH /api/profile` (username uniqueness → `conflict`), `GET/PATCH /api/settings`.
5. Sessions: kinds `lesson`, `practice`, `letters`, `unit_review` (MVP; `legendary`/`jump_test` → `validation` error unless the engine supports them). Enforce level locking (a locked level → `forbidden`), out-of-hearts (`out_of_lives`, practice exempt), learner state for generation (FSRS cards, open mistakes, exposures). `POST /api/sessions/:id/events`: a wrong attempt costs one heart (game-rules; idempotent on (session, attemptSeq); practice exempt; events after completion → `conflict`).
6. `POST /api/sessions/:id/complete`: extend the skeleton with hearts reconciliation at commit (server-counted wrong answers vs recorded events; use the game-rules helpers such as `settleCommit`/`extraHeartsToCharge` if present on main, else implement the rule once in lib/server and cover it with the oracle cases), practice heart reward, SRS updates for every lexeme/letter the session exercised (srs `ratingFor`/`outcomesByItem` + `review`, skipped attempts ignored), enrollment `currentLevelId`, public stats sync, and plausibility checks (e.g. impossible per-answer times, too many sessions per hour → flag in logs and skip XP, never trust client XP). When `@zaboon/session-engine` exports `gradeResponse` (ws-engine), use it and delete `regrade.ts`.
7. Account: `POST /api/account/merge` (member only: verify the guest token with the same verifier as `authenticate`, then `repos.merge.mergeGuestIntoMember` under withSystem; idempotent), `GET /api/account/export`, `DELETE /api/account` (deletes the auth user through an `AuthAdmin` interface: Supabase Admin API with SUPABASE_SECRET_KEY in production, the dev-users store in local mode; data cascades).
8. Reports: `POST /api/reports`, admin `GET /api/admin/reports` and `PATCH /api/admin/reports/:id`.

TESTS (required):
- Route tests as Vitest DB tests in `apps/web/tests/api/*.db.test.ts` that import the route modules and call their handlers directly with `Request` objects against a `createTestDatabase()` clone. Build a small harness in `apps/web/tests/api/harness.ts`: set `AUTH_MODE=local`, `DATABASE_URL_APP_SERVER=<clone>` and `ZABOON_CONTENT_DIR=<temp dir>` before importing handlers, publish `content/fixtures` into the temp dir + clone with the content CLI's `publishLocal` (`tools/content-cli/src/publish.ts`, deep import is fine), mint tokens with `signLocalToken`, and time-travel with the `x-test-now` header.
- Every `:id` route gets a cross-user (IDOR) test; every write gets an idempotency or replay test; concurrent `/complete` calls for one session lose nothing and award XP once; streak day 2 / freeze / broken and heart regeneration via time travel; guest → member merge keeps the stronger FSRS rows and sums XP; export contains every table's rows for the user and nobody else's; delete leaves no rows behind.
- Keep `e2e/skeleton.api.spec.ts` green (it runs in `pnpm verify`).

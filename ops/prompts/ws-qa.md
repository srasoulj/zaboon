SPEC: `ws-qa` (this session: `ws-qa-1`, branch `claude/zaboon-ws-qa-1`) — acceptance and regression tests for everything on main now, plus precise bug reports.

Read: CLAUDE.md (rule 9: tests against the running app); docs/ARCHITECTURE.md §6 (game rules), §7 (API, rate limits, anti-cheat), §10 (key flows), §15 (acceptance tests); docs/LEARNING-ENGINE.md §6 (the 13 MVP challenge types) and §7 (grading); `e2e/fixtures/index.ts`, `e2e/global-setup.ts`, `e2e/skeleton.api.spec.ts` (API answer helpers), `e2e/lesson/**` (player flows — they use a TEST renderer map), `e2e/renderers/**` (how each real renderer is operated), `apps/web/components/challenges/fixtures/samples.ts` (how a correct response is derived from each challenge), `apps/web/tests/api/harness.ts` (DB-level route tests), `content/fixtures` (the frozen course: `u01-s0` = 4 types, `u01-l1` = the 8 course types, `u01-l2` = the 5 letter types).

Write ONLY new files under `e2e/qa/**` and `apps/web/tests/qa/**` — other workers are changing other test folders in parallel. You never change app code.

DELIVER
1. Real-renderer golden paths `e2e/qa/golden.spec.ts` (desktop + mobile Chromium): play `/lesson?course=fixture&kind=lesson&level=<u01-s0|u01-l1|u01-l2>` through the REAL renderers (never set `localStorage['zaboon.testRenderers']`), answering each challenge correctly from the `POST /api/sessions` response (choice → the right card; bank → tiles in solution order; type → the canonical text; pairs → each matching pair; letter_intro → continue; build_word → letters in order), waiting ~900 ms per answer (the anti-cheat awards no XP to faster answers). Assert the complete screens and that `/api/home` XP rises. In one level, one deliberate wrong answer costs a heart and re-queues the challenge. Together these cover all 13 MVP types.
2. ARCHITECTURE §15 items as API specs (`e2e/qa/*.api.spec.ts`, the browserless `api` project) or DB tests (`apps/web/tests/qa/*.db.test.ts`): a `/complete` replay returns the identical result and awards XP once; concurrent completes lose nothing; an IDOR sweep over every `:id` route and every user-scoped read (another user's ids → 404/403, never data); time travel with `x-test-now` (streak day 2, a freeze used, a broken streak, heart regeneration, session expiry → `gone`); rate limits → 429 with the error envelope; an outdated `x-zaboon-app-version` → 426.
3. axe on `/lesson` (answering, feedback, complete screens), tags wcag2a, wcag2aa, wcag21a, wcag21aa, wcag22aa; RTL: every `[lang="fa"]` has `dir="rtl"` and no Persian word is split across elements.
4. Offline: a completion made offline is replayed after a reload on a NON-lesson page (the outbox replayer is now mounted app-wide in `providers.tsx`).
5. For every real bug: open a GitHub issue in srasoulj/zaboon (title `[qa] …`, labels `bug` and `severity:blocker|major|minor`, steps, expected vs actual, evidence) and add a regression test marked `test.fail()` with a comment linking the issue (never `.skip`/`fixme`). Do not fix app code. List the issues in the PR.

Tests must be deterministic (unique data per test, no reliance on order or on fa-en text), fit the `pnpm verify` budget (keep the whole QA suite under about 2 minutes), and pass on a clean clone.

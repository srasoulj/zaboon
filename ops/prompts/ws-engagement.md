SPEC: `ws-engagement` (Wave 3) — leagues, daily quests, coins and the shop, and the practice hub, behind their feature flags (`leagues`, `quests`, `shop`, `practiceHub`; all off by default, and with them off the app must behave exactly as today).

Read: CLAUDE.md; docs/ARCHITECTURE.md §5.2 (P2 tables), §6 (game rules: leagues, quests), §7 (API), §8 (scheduled jobs), §9 (security: flagged users are excluded from leagues), §10.6 (league rollover); docs/DESIGN-SYSTEM.md §2.1 (right rail: league, daily quests, "create a profile"), §2.3 (lesson-complete sequence → league change → quest progress; Leaderboard, Quests, Shop screens), §4.1 (palette: coins = Lājvard, quests = Pesteh), §6 (StatPill popovers: heart refill), §8; docs/adr/0007 (hearts); READ-ONLY seams: `packages/contracts/src/{routes,schemas}.ts` (the P2 routes `leaderboard` (member), `quests`, `shop`, `purchase` and `refillLives` (bucket `shop`), `practice`, `leagueRollover` (GET, `cron`); `LeaderboardResponse`, `LeagueSummary`, `QuestsResponse`/`QuestDto`/`QuestMetric`, `ShopResponse`/`ShopItem`/`ShopUnavailable`, `PurchaseRequest`/`RefillLivesRequest`/`PurchaseResponse`, `PracticeMode`/`PracticeResponse`, `LeagueRolloverResponse`; the optional `HomeResponse.coins/league/quests`, `SessionResult.coins/league/quests` (with `justCompleted`) and `CreateSessionRequest.mode`; `ErrorCode` `insufficient_coins` (409); `AppConfig` `shop.prices`, `quests.rewardCoins`, `leagues.rewardCoins`, `leagues.cohortSize` ≤ 30), `packages/game-rules/oracles/engagement.yaml` + `engagement.oracle.test.ts` (65 cases; the YAML header documents the API) and `leagues.yaml`, `apps/web/lib/server/{with-route.ts,flags.ts,config.ts}` (`ctx.flags` = configured flags plus `x-test-flags` in local mode; `resetRuntimeConfig()`), `apps/web/tests/api/harness.ts` (`h.call(…, { flags })`, `h.setFlags()`, every rate-limit bucket raised), `e2e/fixtures/index.ts` (`setTestFlags(page, flags)`, `flagsHeader()`, `cronHeaders()`, because the local e2e server runs with a fixed fake cron credential from `e2e/fixtures/cron.ts`), `apps/web/components/shell/AppShell.tsx` (flag-gated Leaderboards/Quests/Shop nav items, the coins pill when `home.coins` is present, and the two slots you fill), `apps/web/vercel.json` (weekly rollover cron), `apps/web/lib/api-client.ts` (`queryKeys.leaderboard/quests/shop/practice`). Code you now own and extend: `packages/game-rules/src/**` (the stubs in `src/engagement.ts`; `QUEST_TEMPLATES`, `dailyQuests`, `questIncrement`, `placeInCohort`, `rolloverCohort`), `apps/web/lib/server/{home,sessions}.ts`, `apps/web/app/api/sessions/**`, `apps/web/components/lesson/**`, `apps/web/lib/lesson/{machine,summary,request}.ts` + `machine.test.ts` + `pure.test.ts`, `apps/web/components/practice/**`, the slots `apps/web/components/engagement/{Rail.tsx,hearts-popover.tsx}`, `packages/db/src/{schema.ts,repos/index.ts}`. Existing DB code to reuse: `packages/db/src/repos/merge.ts` already merges wallet, coin_ledger (by (reason, ref)), user_quests and user_league; `repos.account.exportAccount` exports every table with a `user_id` automatically.

DELIVER
1. Game rules (`packages/game-rules/src/engagement.ts`): implement the stubbed API exactly as the oracle header documents (`leagueWeek`, `rolloverPlan`, `leagueXpStep`, `leagueRewardCoins`, `leagueGrant`, `questGrant`, `applyCoinGrants`, `applyQuestProgress`, `purchase`, `shopItems`), then flip `ENGAGEMENT_IMPLEMENTATION` to `'real'`: all 65 engagement cases and the existing oracles must pass. Rename the `QUEST_TEMPLATES` ids to the quest_defs rule (`xp-20` → `xp_20`, …; `QuestDto.id` enforces `^[a-z0-9_]{1,40}$`) and add a drift test that the seeded `quest_defs` rows equal `QUEST_TEMPLATES` (id, template = metric, target, reward = `DEFAULT_APP_CONFIG.quests.rewardCoins`).
2. Database (your migration `supabase/migrations/20260926000000_engagement.sql`, pgTAP `supabase/tests/005_engagement.sql`, `schema.ts`, repos `packages/db/src/repos/{leagues,quests,wallet}.ts` with `*.db.test.ts`; expand-only). Blockers to fix there: (a) `league_weeks` is writable only in system scope, but the first XP of a week happens in a user-scoped commit: add a user-scope INSERT policy (commits create the week row with `INSERT … ON CONFLICT (starts_at) DO NOTHING`; closing stays system-only) and a CHECK that `starts_at` is a Monday 00:00 UTC and `ends_at = starts_at + 7 days`; (b) `user_quests.quest_id` references `quest_defs`, which is empty: seed `quest_defs` from the templates; (c) the `quest_defs.id` CHECK rejects the current template ids (see 1). Keep `supabase/tests/001–004` and `packages/db/src/drift.db.test.ts` green (they are not yours: e.g. 003 inserts week 2026-09-21 and quest def `earn_xp`).
3. Commit (`POST /api/sessions/:id/complete` in `lib/server/sessions.ts`), only while the matching flag is on, inside the SAME `withUserLock` transaction as the XP ledger: league XP (`leagueXpStep`; the week is the one containing `ctx.now`, never a closed one; joining places the learner with `placeInCohort` under `pg_advisory_xact_lock` on (week, tier) and bumps the cohort size; guests never join; tier from `user_league`, default `mes`), quest progress with auto-claim (`applyQuestProgress` on the day's `dailyQuests(userId, localDate, cfg)` for the session's local date), coin grants (`applyCoinGrants` → `wallet` + append-only `coin_ledger`, idempotent by `UNIQUE(user_id, reason, ref)`). Anti-cheat flagged sessions (`plausibilityFlags`) advance no quest, no league and no coins. Return `SessionResult.coins/league/quests` only for flags that are on; a replay returns the stored result verbatim.
4. Reads and writes (new route files under `apps/web/app/api/{leaderboard,quests,shop,lives,practice,cron}/`, logic in `apps/web/lib/server/engagement/`), each answering 404 `not_found` while its flag is off: `GET /api/leaderboard` (members only: the learner's cohort in rank order with zones from `rolloverCohort`'s counts, public profile fields only — never user ids; `joined: false` and no members before the first XP of the week; `lastResult` from last week's `final_rank`/`outcome`); `GET /api/quests` (today in the profile timezone, `resetsAt` = next local midnight); `GET /api/shop` (`shopItems`); `POST /api/shop/purchase` and `POST /api/lives/refill` (`purchase` in one `withUserLock` tx: wallet + one negative ledger row whose ref is the purchaseId (a replay or reuse is found by that ref and never charged twice) + streak freezes / lives; refusals: `insufficient_coins` → `insufficient_coins`, `max_owned` / `lives_full` / `unlimited_lives` / `purchase_id_reused` → `conflict` with `details: { reason }`); `GET /api/practice` (modes with availability and counts, e.g. open mistakes); `GET /api/cron/league-rollover` (Vercel Cron, `withSystem`: `rolloverPlan`, then per closed week and cohort `rolloverCohort` → `final_rank`, `outcome`, `user_league` tier, `leagueGrant` coins, `closed_at`; idempotent; open the current week). `GET /api/home` adds `coins` (flags shop or quests), `league` (flags leagues, linked accounts) and `quests` (flags quests).
5. UI (only while the flags are on): `/leaderboard` (tier banner with the Persian gem/metal names, cohort list with promotion/demotion zones and "you", countdown to `week.endsAt`, last-week result banner; guests see "Create a profile to join leagues"), `/quests` (today's quests with progress bars, reset countdown), `/shop` (coin balance, streak freeze and heart refill with price, owned/max and the disabled reason; purchases send a fresh `crypto.randomUUID()` purchaseId per tap and reuse it on retry), the practice hub on `/practice` (flags.practiceHub: mode cards from `GET /api/practice` → `/lesson?course=…&kind=practice&mode=<mode>`; extend `request.ts`/`machine.ts` so the mode reaches `createSession`; with the flag off the page stays as it is), the right-rail cards in `components/engagement/Rail.tsx` (league card, daily-quests card, "create a profile" card for guests) and the hearts popover in `components/engagement/hearts-popover.tsx` (hearts, next regeneration, "Refill for 150" → refillLives; return `undefined` while flags.shop is off so the pill stays exactly as it is), and the lesson-complete sequence (league change → quest progress, from `SessionResult`; nothing new when the fields are absent). Invalidate `queryKeys.home/leaderboard/quests/shop` after lessons and purchases.

6. Inherited from ws-player (you now own the lesson player):
   - **Transcript alignment:** after CHECK, the feedback bar left-aligns the Persian transcript in its row while the RTL answer line is right-aligned. Render it as an RTL island (`lang="fa" dir="rtl"`, `text-align: start`). Found by the media QA.
   - **Enter handling (keep it):** Enter becomes CHECK only on a selected `.zb-choice` card while a draft exists (`isInteractive` in LessonPlayer.tsx). Every other focused control keeps its own Enter; e.g. a placed build_word tile removes its letter. Keep the DOM tests green.
   - **Outbox:**
     - The outbox sends only through `identitySender`: `{ userId, token }` come from one `getSession()` of the mounted replayer's auth.
     - A mismatch is `blocked`, never permanent.
     - Keep `sharedLessonStores(api)` and `retagOutboxUser` signatures (ws-pages uses them).
   - **The cron route:** `apps/web/vercel.json` already schedules `GET /api/cron/league-rollover` (Mondays 00:00 UTC, `Authorization: Bearer $CRON_SECRET`). Build it with `withRoute(routes.leagueRollover, …)` so the cron auth applies; until then the cron gets a 404.
   - **Two `NotImplementedError` classes:** `@zaboon/game-rules` exports one from the engagement stub, and `lib/server/sessions.ts` maps only session-engine's to a 400. With the real engagement API the stub never throws; don't let it reach a route.
   - **Local cron e2e specs:** they need the dev server Playwright starts (its env has a fake `CRON_SECRET`). A dev server you started yourself on the e2e port gets reused (`reuseExistingServer`), and without the secret its cron calls answer 401.
   - **Practice audio:** the practice hub plays word audio through `components/path/play-audio.ts` (ws-path-letters'). It only plays content-origin URLs: same-origin `/content/…` or `NEXT_PUBLIC_CONTENT_BASE_URL`, which next.config.ts inlines from `CONTENT_BASE_URL`. Keep using it.

RULES AND PITFALLS
- Flags off = today's app, byte for byte: no new home/result fields, no P2 writes at commit, P2 routes 404, the shell and the lesson flow unchanged (`e2e/shell.spec.ts`, `e2e/lesson/**` and every existing DB test keep passing).
- Money: every wallet change is a `coin_ledger` row in the same transaction (the balance always equals the ledger sum); grants and purchases are idempotent by (reason, ref); never let `wallet.coins` go negative (the CHECK); purchases and refills run under `withUserLock`.
- Locks: always user lock → shared week lock (`pg_advisory_xact_lock_shared` on the week) → exclusive (week, tier) lock for placement. Take the week lock before any wallet or league write in the commit. The rollover takes the week lock exclusively and never takes user locks, so a commit and a rollover can't deadlock and a closed week never receives XP.
- Rollover is idempotent (`closed_at`), closes missed weeks oldest first, and is safe to run twice at once.
- Leagues: linked accounts only (§6); other learners appear only through `public_profiles` fields; RLS already limits league_members reads to the caller's cohort — still filter by the caller.
- Every `:id`-style or user-scoped read needs a cross-user (IDOR) test; time comes from `ctx.now` (`x-test-now` in tests), never `new Date()` in rule code.
- `packages/game-rules` stays framework-free; one implementation per rule (the client only displays server numbers).
- Don't edit the seams listed as READ-ONLY; for a contract change keep an adapter on your side and add `## Contract change request`.
- Shared with ws-typing (working in parallel): the session engine (`packages/session-engine/src/**`) and grading (`apps/web/lib/lesson/grading.ts`) are theirs. `GenerateInput.practiceMode` is implemented by ws-typing; until it lands every practice mode builds today's practice session, so your practice hub works end to end either way. ws-typing may change `apps/web/components/challenges/**` and the `translate_type` renderer; the lesson player is yours — keep `ChallengeDisplay.keyboardLayout/persianKeyboard` flowing from settings and `home.flags` (LessonRoute already passes them), and treat a declined trace (`{kind:'trace', declined: true}`, graded `correct`) like any correct answer.
- The golden-session oracle (`packages/session-engine/oracles/`) pins MVP sessions: don't change what `createSession` passes to the engine when every flag is off.

TESTS
- unit: the engagement oracle (flip `ENGAGEMENT_IMPLEMENTATION`), quest-template drift, the league-week / reset-time helpers.
- DB (`apps/web/tests/engagement/*.db.test.ts` with the harness; `packages/db/src/repos/*.db.test.ts`; pgTAP 005): with flags off nothing changes (home/result shapes, no P2 rows written); first XP of a week joins a cohort, a 31st learner opens a new cohort, concurrent first-XP commits for one (week, tier) never exceed 30 or fragment cohorts; weekly XP and quest progress inside `/complete` (replay awards nothing twice; flagged sessions advance nothing); quests auto-claim once per local date (time travel across midnight in two timezones); purchases (success, each refusal, replayed purchaseId, reused purchaseId → 409, concurrent double purchase charges once) and the ledger = balance invariant; rollover (promotions/demotions/tiers/reward coins, idempotent on a second call, missed weeks, 401 without the cron secret); leaderboard IDOR (no user ids, only your cohort, 403 for guests); guest → member merge keeps coins and quests.
- dom: shop, quests, leaderboard, rail cards, hearts popover, complete-screen additions, practice hub (flags on and off).
- e2e (`e2e/engagement/`, flags via `setTestFlags` / `flagsHeader`): earn XP → leaderboard shows you; complete a quest → coins in the stats bar; buy a freeze; refill hearts from the popover; rollover via `GET /api/cron/league-rollover` with `cronHeaders()`; axe + RTL checks on the new pages; Chromium-only screenshot baselines (early `return` on other browsers, never `.skip`).
- Run `pnpm --filter @zaboon/web build` once before your PR.

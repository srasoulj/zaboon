SPEC: `ws-db` — complete the database: all remaining tables, policies, repositories and pgTAP tests.

Read: docs/ARCHITECTURE.md §5 (data model: every MVP and P2 table), §6, §8 (jobs), §9 (security), docs/adr/0009; `supabase/migrations/20260925000000_base.sql` and `…100_core.sql` (READ-ONLY: never edit applied migrations; add new ones); `supabase/local/shim.sql`; `packages/db/src/*` (withUser/withUserLock/withSystem are the contract; the isolation model: user-scoped tables get policy `TO app_server USING (rls.can_access(user_id))`, shared tables read-all + system-write).

DELIVER
1. New migrations (Supabase CLI naming `YYYYMMDDHHMMSS_name.sql`, timestamps after 20260925000100) for every table in ARCHITECTURE §5 not yet created: consents, level_progress, session_answers, user_items, lexeme_memory, letter_memory, mistakes, reports, item_stats, rate_limits (token bucket: key, tokens, updated_at), and P2: wallet, coin_ledger, league_weeks, league_cohorts, league_members, user_league, quest_defs, user_quests, entitlements, webhook_events, push_subscriptions. Proper PKs, FKs (ON DELETE CASCADE to profiles), CHECKs, indexes; RLS + REVOKE from anon/authenticated + the right app_server policies on every table. Column names must match the contracts' DTO semantics.
2. `internal.*` housekeeping SQL functions (prune session_answers > 90 days, roll up item_stats, expire stale sessions) and pg_cron schedules guarded with `IF EXISTS (SELECT 1 FROM pg_extension WHERE extname='pg_cron')`.
3. Drizzle schema for every table in packages/db/src/schema.ts; typed repository functions in packages/db/src/repos/*.ts that REQUIRE a `userId` (or explicit system scope) for user-scoped access: profiles/settings, enrollments, sessions (+ events, answers), xp_ledger + daily_activity, streaks, lives, memory (lexeme/letter cards), mistakes, level_progress, reports (+ admin list/update under system scope), content_versions/app_config reads, rate-limit token bucket (`consumeToken(key, perMinute)`), account export (all rows for a user as JSON) and delete, guest→member merge (union daily_activity, sum XP, keep stronger FSRS rows, move progress; system scope).
4. pgTAP tests in supabase/tests/*.sql run by a Vitest db test (`packages/db/src/pgtap.db.test.ts` runs `SELECT * FROM runtests()` or `pg_prove`-style per file against a createTestDatabase clone): every table has RLS enabled; anon/authenticated have zero privileges on every public table and cannot execute anything in internal/rls; app_server cannot see other users' rows. Plus Vitest db tests for each repository (including IDOR-style cross-user attempts and idempotency: duplicate session_events, duplicate xp_ledger (session_id, reason)).
5. A `scripts`-free drift check: a db test that introspects information_schema and asserts the Drizzle schema's tables/columns match the migrated database.

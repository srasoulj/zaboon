SPEC: `ws-typing` (Wave 3) — the in-app `PersianKeyboard`, typed Persian challenges (`translate_type` en→fa, `listen_type`, `cloze_type`) and letter tracing (`letter_trace`), behind the flags `persianKeyboard` and `letterTrace` (both off by default; with them off every session must be exactly today's).

Read: CLAUDE.md (rule 5: Persian text); docs/LEARNING-ENGINE.md §1.3 (rendering: RTL islands, the answer line/input direction comes from the answer language, never `dir="auto"`), §1.4 (input: the two layouts, physical-key remapping, touch devices), §1.5 (normalization), §5 (session engine; ladder step 5 "typing, in later levels"), §6 (challenge catalogue, P2 rows), §7 (grader: typo and spelling verdicts for Persian), §8; docs/DESIGN-SYSTEM.md §4.3 (3D recipe), §6 (`PersianKeyboard`), §8 (accessibility, motion); READ-ONLY seams: `packages/contracts` (`TranslateTypeChallenge` with `direction`/`answerLang`, `ListenTypeChallenge`, `ClozeTypeChallenge`, `LetterTraceChallenge` with `form`; `ChallengeResponse` `{kind:'trace', coverage, precision, declined?: true}`; `AppConfig.mixProfiles` categories `typing` and `letterTrace`; `Settings.keyboardLayout`), `packages/session-engine/oracles/` (golden MVP sessions for fixed seeds: must stay green), `apps/web/lib/challenge-registry.ts` (optional `listen_type`/`cloze_type`/`letter_trace` renderer slots; `ChallengeDisplay.keyboardLayout` and `.persianKeyboard`, filled by the player from settings and `home.flags`), `content/fixtures/units/u01-fixture.yaml` (level `u01-t1`, last on the path: translate_type en_fa `s_u01_0003`, listen_type `s_u01_0005`, cloze_type `s_u01_0007`, letter_trace `l_be`), `apps/web/lib/server/sessions.ts` (ws-engagement's: it passes `features: { persianTyping: flags.persianKeyboard, letterTrace: flags.letterTrace }` and, while flags.practiceHub is on, `practiceMode`; it maps `NotImplementedError` to a 400), `apps/web/tests/api/harness.ts` (`h.call(…, { flags })`, `h.setFlags()`), `e2e/fixtures/index.ts` (`setTestFlags(page, flags)`). Code you now own: `packages/session-engine/src/**` (`builders.ts`, `generate.ts` with `GenerateInput.features`/`practiceMode`, `grade.ts`), `packages/farsi/src/{keyboard.ts,keyboard.test.ts,index.ts}`, `packages/ui/src/**` (tokens.json stays read-only), `apps/web/components/challenges/**`, `apps/web/app/(dev)/{gallery,challenges}/**`, `apps/web/lib/lesson/{grading.ts,grading.test.ts}`, `apps/web/lib/typing/**`, `apps/web/tests/typing/**` (incl. `u01-t1.db.test.ts`), `e2e/{ui,renderers,typing}/**`.

DELIVER
1. farsi: `KEYBOARD_ROWS` (the drawing order of `KeyboardEvent.code`s per row for both layouts; every mapped key appears once) and a pure remap helper for physical keys per §1.4: remap only when `event.key` is a single Latin character, `!event.isComposing`, no Ctrl/Meta/Alt, and never for `keyCode 229` (Android IMEs); Shift+Space (`HALF_SPACE_CODE`) types ZWNJ. Keep the existing `KEYBOARD_LAYOUTS` mappings unless a test proves a bug.
2. ui: `PersianKeyboard` in the kit: the standard (ISIRI 9147) and phonetic layouts from `@zaboon/farsi`, long-press variants on phonetic, a labeled half-space key, space, backspace and enter; 3D keys; operable by keyboard and screen reader (each key announces its character; the half-space key says "half-space"); reduced motion; gallery entries.
3. session-engine:
   - Builders for `listen_type` (audio and transcript like listen_tap, typed answer graph), `cloze_type` (before/after tokens, the translation, a graph of the blank's accepted forms) and `letter_trace` (the letter's info; `form` from the ref's variant option: 0 isolated, 1 initial, 2 medial, 3 final; non-connectors only isolated/final). `translate_type` en→fa already builds (`translate()`); the planner never emits it yet.
   - Planner: a `typing` pool (translate_type en→fa over sentences, listen_type, cloze_type) only with `features.persianTyping`, a `letterTrace` pool only with `features.letterTrace` (the categories are already in the mix profiles and dropped while off), ladder step 5 only with the feature. Pinned P2 refs whose feature is off are replaced in place by their MVP twin (translate_type en_fa → translate_bank en_fa, listen_type → listen_tap, cloze_type → cloze_choice, letter_trace → letter_forms of that letter), so u01-t1 plays with the flags off; with a feature on they build as pinned. Stored refs hold what was built, so rebuilds stay exact.
   - `practiceMode` (the practice hub, ws-engagement): `mixed` = today's practice session, byte-identical; `mistakes` = open mistakes only (falls back to `mixed` when there are none); `listening` = listening challenges only (listen_tap, plus listen_type with persianTyping); `typing` = typed answers only (translate_type fa→en, plus en→fa, listen_type and cloze_type with persianTyping).
   - `gradeResponse`: typed Persian for translate_type (answerLang fa), listen_type and cloze_type through the grader with the lexicon (typo/spelling verdicts); `letter_trace` with `{kind:'trace'}` passes when `coverage` and `precision` reach exported thresholds (e.g. `TRACE_MIN_COVERAGE` 0.7, `TRACE_MIN_PRECISION` 0.6; tune on real strokes); `declined: true` ("Can't trace now") grades `correct`, so it costs no heart and is not re-queued (documented trade-off: it counts as a clean review of that letter); any other response is wrong; it still never throws.
4. Renderers: `translate_type` handles `answerLang: 'fa'` (an RTL textarea with `lang="fa" dir="rtl"`; the in-app `PersianKeyboard` in `display.keyboardLayout` when `display.persianKeyboard`, with `inputmode="none"` on touch devices; physical keys remapped per §1.4); `ListenType`; `ClozeType` (an inline blank between whole-word tokens); `LetterTrace` (a pointer canvas with Pointer Events and `touch-action: none`; the guide is the letter form rendered with the app's Persian font; strokes are scored on the client in `apps/web/lib/typing/`, e.g. rasterize the glyph, dilate it, coverage = share of the glyph's skeleton near a stroke, precision = share of stroke points inside the dilated glyph; it reports `{kind:'trace', coverage, precision}`; a "Can't trace now" button reports `{kind:'trace', coverage: 0, precision: 0, declined: true}` and submits). Register them as the optional entries of `renderers`; dev gallery pages for every P2 challenge (re-record `apps/web/components/challenges/fixtures/fixture-challenges.json` including u01-t1).
5. u01-t1 end to end with the flags on: it builds the four P2 challenges, the player renders them, the server re-grades them on `/complete`.

6. Character portraits. `CompleteChatChallenge.speaker` now has an optional `image`: a media URL that the engine fills from the course's `characters.yaml`. The fa-en cast has transparent WebP portraits; the fixture cast has none.
   - The kit's `Character` draws `image` as an `<img>` with the character's name as `alt`, at the placeholder's size and crop, with no layout shift.
   - It falls back to the SVG placeholder when there is no image or the image fails to load.
   - The complete_chat renderer passes `speaker.image` through.
   - Tests: a DOM test for both cases, plus gallery entries.
7. Nits inherited from ws-renderers (from the #37 review; fix them while you're in these files):
   - `MatchColumns`: set the live-region text immediately when it differs from the current text, and clear-then-set only for a repeated message, so "All pairs matched." isn't delayed behind the feedback bar. Consider 100 ms instead of 50 ms. Fix the "one tick" comment.
   - `LetterForms.test.tsx`: check `lang` on the element that holds the text (`within(name).getByText(…).closest('[lang]')`), not on the button.
   - The Enter tests in `MatchPairs.test.tsx` and `SelectTranslation.test.tsx`: assert `aria-pressed="true"` before the second Enter, and remove listeners and observers in `try/finally`.
   - `SelectImage.tsx`: load the images with `crossOrigin="anonymous"`. Without it, CDN images come back opaque, and the service worker's CacheFirst only stores 200 responses, so lesson pictures are never cached offline in production.

RULES AND PITFALLS
- With every feature off, sessions are byte-identical to today's: `packages/session-engine/oracles/mvp-sessions.oracle.test.ts` (read-only) must stay green. Don't reorder existing pools or draw extra randomness while a feature is off; add P2 pools after the MVP ones.
- Persian text: never split a word across elements (the typed-answer echo, the cloze blank and feedback diffs are whole words); set `lang="fa" dir="rtl"` explicitly on Persian inputs; keep ZWNJ (U+200C) intact; never normalize the learner's input in the UI (the grader normalizes both sides).
- IME: remap by `event.code` only for a Latin `event.key`, never while composing and never on keyCode 229; OS Persian layouts type unchanged; the on-screen keyboard inserts at the caret (selection-aware) and supports undo; Enter submits (`onSubmit`), Shift+Enter is a newline.
- Tracing is scored on the client, and the server re-grades the scores with the same thresholds in `gradeResponse`. A declined trace needs no special case in the player or the server.
- Don't edit `packages/grader`, the oracles, the contracts or the registry; for a contract change keep an adapter on your side and add `## Contract change request`.
- Shared with ws-engagement (working in parallel): the lesson player (`apps/web/components/lesson/**`, `lib/lesson/{machine,summary,request}.ts`), sessions and home on the server are theirs. They send `practiceMode`; until it lands in the engine every mode builds today's practice session.

TESTS
- unit: builders for the three P2 types (schema-valid, deterministic, `rebuildChallenges` exact), P2 pools only with their feature, MVP twins with the feature off, practiceMode semantics (`mixed` identical to no mode), `gradeResponse` for traces (thresholds, declined, other kinds) and typed Persian (typo, spelling, a lexicon word → wrong), `KEYBOARD_ROWS` covers every key of both layouts, the remap helper, the trace scorer on synthetic strokes (a clean trace passes, a scribble and an empty canvas fail).
- property: extend the fixture and fa-en property runs with the features on (schema-valid, deterministic, rebuildable).
- dom: `PersianKeyboard` (both layouts, long-press variants, half-space, keyboard and screen-reader use), each new renderer (correct → graded correct, wrong → graded wrong, keyboard-only, feedback lock), `translate_type` en→fa with and without the in-app keyboard.
- DB: flip `apps/web/tests/typing/u01-t1.db.test.ts` (flags on: u01-t1 builds the four P2 challenges and completes, re-graded on the server; flags off: the MVP twins).
- e2e: `e2e/typing/` (typed Persian with the on-screen keyboard and with physical keys; tracing with synthetic pointer strokes; "Can't trace now" keeps hearts), plus `e2e/renderers/` and `e2e/ui/` (axe tags wcag2a, wcag2aa, wcag21a, wcag21aa, wcag22aa; every `[lang="fa"]` has `dir="rtl"`; Chromium-only screenshot baselines with an early `return` on other browsers, never `.skip`).
- Run `pnpm --filter @zaboon/web build` once before your PR.

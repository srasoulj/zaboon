SPEC: `ws-renderers` — build the 13 MVP challenge renderers the lesson player shows.

Read: CLAUDE.md (esp. rule 5, Persian text), docs/DESIGN-SYSTEM.md (all: §2.2 lesson screen, §4.3 3D recipe, §5 typography, §6 component kit, §8 sound/motion/accessibility), docs/LEARNING-ENGINE.md §1 (Persian text: joining, ZWNJ, RTL, transliteration fade), §6 (challenge catalogue); `apps/web/lib/challenge-registry.ts` (READ-ONLY: the renderer contract — props, what the player owns vs what a renderer owns); `packages/contracts/src/schemas.ts` (the `Challenge` union: every field you render); `packages/ui` (use the kit: ChoiceCard, TileBank/WordTile, FaText, SpeechBubble, Character, Button3D, icons; do not re-implement them); `packages/session-engine/src/grade.ts` (`gradeResponse`: the response shape each type must produce); the fixture course `content/fixtures` (u01-l1 pins the 8 course types, u01-l2 the 5 letter types).

DELIVER — replace the placeholders in `apps/web/components/challenges/` with one component per type, exported from `index.ts` as `renderers: RendererMap` (the registry file is read-only):
1. Duolingo-faithful layouts in OUR palette and kit: the instruction heading ("Select the correct meaning", "Write this in English", "Tap what you hear", "Select the matching pairs", …), the prompt (Persian via `FaText`: `lang="fa" dir="rtl"`, transliteration under each word when `display.transliteration`, vowel marks when `display.vowelMarks`; a speaker button that plays the prompt audio when present), the answer area, all mobile-first and centered (max ~640px).
   - select_image: 2×2 image cards with number hints; select_translation / cloze_choice / letter_sound / read_word: `ChoiceCard` lists with 1–9 shortcuts (`useDigitShortcuts`); translate_bank and listen_tap: `TileBank` (answer line direction from the answer language; Persian tiles are whole words); listen_tap and letter_sound audio: big speaker + turtle (0.7×) buttons via `props.audio.play(url, { slow })`; translate_type: an auto-growing textarea (Enter submits via `onSubmit`, Shift+Enter newline), `lang="en"`; match_pairs and letter_forms: two columns of tap-to-match buttons (Persian column RTL), matched pairs lock and fade, a wrong pair shakes and calls `onMismatch()`, and when all are matched report `{kind:'pairs'}` and call `onSubmit()`; complete_chat: the speaker's `Character` + `SpeechBubble` with the prompt, reply `ChoiceCard`s; letter_intro: a big letter card (name, sound, the four joined forms rendered with ZWJ so they actually connect, example words with audio) that reports `{kind:'none'}` immediately; build_word: letter tiles that visibly JOIN as they are placed (render the assembled word as ONE text run, never one span per letter), the target meaning shown.
   - In `phase === 'feedback'` inputs are locked; choice renderers highlight the chosen and correct options (never before CHECK).
   - Reduced motion (`display.reducedMotion` and the OS setting): no shake/fly animations.
2. Keyboard and screen readers: every interaction works with the keyboard alone; choices are radio-like (`aria-pressed`/`aria-checked`), tiles are buttons with their word as the name, audio buttons have names ("Play audio", "Play slowly"); Persian elements carry `lang="fa" dir="rtl"`.
3. A dev gallery `apps/web/app/(dev)/challenges/page.tsx` that renders every fixture challenge (build them from content/fixtures with `@zaboon/session-engine`'s builders on the server side of the page, or from recorded JSON) in answering and feedback states, light and dark, desktop and mobile widths; hidden in production like the UI gallery (`notFound()` unless dev).
4. Tests: a dom test per renderer (`*.test.tsx`, Testing Library + user-event): it renders the fixture challenge, the correct interaction produces exactly the response that `gradeResponse` grades `correct`, a wrong interaction produces one graded `wrong`, keyboard-only use works, and feedback-phase locking; plus Playwright specs in `e2e/renderers/` over the gallery (axe-core accessibility checks with @axe-core/playwright if available, else role/name assertions; screenshot baselines for chromium only).
